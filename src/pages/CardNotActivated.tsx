import CardNotActivated from "../components/CardNotActivated";

const CardNotActivatedPage = () => {
  return (
    <div className="min-h-screen w-full bg-black">
      <CardNotActivated />
    </div>
  );
};

export default CardNotActivatedPage;
